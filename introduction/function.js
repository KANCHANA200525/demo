/**
 * javascript function defined with function keyword 
 * followed by name by parameter
 * 
 * syntax:
 *function name(parameter1,parameter2,)
 * 
 * 
 * statememt;
 * return variable_name;
 * }
 */

 let a= data1(5,8,6);
 console.log(a);

 function data1(x,y,z){
  return x+y+z;
 }

 /**
  * note:
  * if the variable declare inside the function called local variable
  * if declared outside means gobal variable
  */
  // local variable
  function example(){
     let bikeName="ducati";
     console.log(bikeName);

  }
  example1();


  /**
   * before arrow function 
   */



/**
 * after using arrow function
 */
let data=(a,b,c)



  /**
   * arrow function with parameter
   */

  let sap="";
  sap=(val)=> "java"+val;
  console.log(sap("script"));